# Expo inlines anything prefixed EXPO_PUBLIC_ at build time.
# "localhost" only works in the iOS simulator. For a physical device or the
# Android emulator, use your machine's LAN IP (or an Expo tunnel) instead.
EXPO_PUBLIC_API_URL="http://localhost:4000"
